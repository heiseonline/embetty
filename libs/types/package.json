{
  "name": "@embetty/types",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint --cache .",
    "build": "tsc --build tsconfig.build.json"
  },
  "devDependencies": {
    "@embetty/eslint-config": "workspace:*",
    "@embetty/tsconfig": "workspace:*",
    "tslib": "^2.4.0"
  }
}
