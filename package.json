{
  "name": "@embetty/monorepo",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "prepare": "husky install",
    "bump": "changeset version",
    "release": "changeset publish",
    "lint": "pnpm run -r --stream lint --quiet"
  },
  "dependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.0.3"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "@commitlint/cli": "^17.6.3",
    "@commitlint/config-conventional": "^17.6.3",
    "@embetty/eslint-config": "workspace:^1.0.0",
    "@embetty/tsconfig": "workspace:^1.0.0",
    "@types/jest": "^29.5.0",
    "@types/node": "^18",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "prettier": "^2.8.8"
  }
}
